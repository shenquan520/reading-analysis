# 验证规约（VERIFICATION）

> **给谁看**：维护这个 skill 的人 / 模型。
> **为什么单独一篇**：`SKILL.md` 讲「怎么交付」，本文件讲「**怎么证明你的改动是对的**」。
> 这些纪律每一条都是从一次真实事故里长出来的，不是设计出来的。
>
> **一句话**：**「改了」不算完成，输出为绿才算——而且那个绿必须能骗过不信你的人。**

---

## 一、对比实验：基线必须**独立实现**（2026-09-24 第十轮立）

做「旧方案 vs 新方案」的对比时，**旧方案那一列要单独写一份实现，禁止直接调产品代码**。

| | 做法 | 后果 |
|---|---|---|
| ❌ 错 | 基线列调 `ap.agent_ok()`（产品函数） | 产品一改，基线**跟着变** → 对比表显示「新方案更好」，其实只是基线被换了 |
| ✅ 对 | 基线列复刻一份冻结版实现 | 表格才有信息量 |

**真实案例**：本 skill 第九轮做施事判定方案对比时，「现行方案」一列最初直接调产品代码。当时产品已改成新实现，
于是那一列显示 **0 误差**（真实值 2），对比表差点变成一张「新方案与现行一样好」的废纸。
改动前**先跑一次记录数值**，或用独立实现——两条任选一条，但**必须有**。

> 配套要求：**报数要带样本来源与规模**。「误差 0/104」只有在写明「含外部审核方 22 条原句」时才有分量。

---

## 二、每轮必做：跨轮回归核验（2026-09-24 第十轮立）

```bash
python scripts/cross_round_check.py          # 8 项，全绿才继续
```

**查的不是「当初修没修」，而是「后面若干轮大改了同一批文件，有没有碰回去」。**
单轮审计看不见这类问题——它是**改动之间的踩踏**。

真实案例（都是本项目发生过的）：

| 事故 | 谁发现的 | 代价 |
|---|---|---|
| 第七轮加「施事判定」清掉 8 条误报 → **把中文话题句 5/5 全杀** | 第八轮 | 一类漏报存活一轮 |
| 第八轮把 fixture 字段从 `passage` 挪进 `note` → **契约缺口照旧被掩盖** | 第九轮 | 同一处**两轮复发** |
| 第五轮报告当时没被处理 → 那 9 条样本**一直没入库** | 第九轮翻目录 | 样本缺失四个轮次 |

**维护纪律**：`cross_round_check.py` 的 `CHECKS` 列表**追加式、永不删除**，
每轮的判据加进去并注明轮次。它是「判据的回归集」，`ap_cases.py` 是「样本的回归集」，两者缺一不可。

> ⚠️ **报告可能不被投喂**：开工前先扫一遍报告目录，别只处理手边那份。

---

## 三、用例集：追加式、分来源标注

| # | 纪律 | 为什么 |
|---|---|---|
| 1 | 独立成文件（`scripts/ap_cases.py`），不硬编码进脚本 | 让人能看见样本、能被外部替换 |
| 2 | **追加式**：每轮发现的失败用例进去**永不删除、永不改写** | 脚本才从「自证清白」变成「回归防线」 |
| 3 | **分来源标注**（外部审核 / 实战 / 自造）分开跑、分开报数 | 自造样本全绿**不代表**外部的能过 |

**反面教材**：曾用「自造且已按关键词调过」的样本报「漏报 0/15」，外部审核方换自己的一套测，召回只有 5/9。
**危害不是数字错，是让下一个人停止检查。**

---

## 四、门禁报数必须如实：口径要说清，不能只报「0」

- 同一套样本**两种口径都要报**（生产口径 / 关闸裸测），只报一种 = 假绿。
  实测：只报关闸口径时「现实现实中性样本 0 误报」是真的，但**概念名试纸 23/26 会被打中**被藏了起来。
- 门禁语义分三档并写清：`dual`（两种口径都必须 0）／ `prod`（生产口径必须 0）／ `diag`（只报数，不设阈值）。
- **「没查」不等于「通过」**：检查因故没执行（没配配置、被 profile 豁免）必须**如实报没查**，
  静默绿着就是假防护。

---

## 五、判据 / 根因结论：必须标注「已实测 / 未实测」

任何「根因判断」「候选修法」「建议」，一律带三件套：

1. `已实测` / `未实测`
2. 测在**哪个样本集、多少条**
3. 用来实现它的**确切判据**（不能只给一句概括）

**真实案例**：一次报告把某误报的根因判成「距离太远」，用词肯定但**没跑过对照**——
实测把窗口放宽到 40 只改善 1/4，真因是「词表没覆盖」。另一次建议的判据「一句话概括」与其实际
模拟实现不一致（实际还多要求「句中存在对象词」），照字面实现会把两类合法输入打坏。

> **一句话概括的判据 ≈ 不可复现的判据。** 要么给确切规则，要么标 `未实测`。

---

## 五点五、验实物要验到**元素级**，不能只 grep 关键字（2026-09-24 第十一轮立）

「验实物，不信自述」还不够——**实物本身也会骗人**。

| 口径 | 结果 |
|---|---|
| ❌ `grep "ap-layer"` / `grep "反模式层"` | **各命中 1 次**——那是渲染器**恒定注入的 CSS**（`.ap-layer{…}` 与 CSS 注释），**不代表弹了层** |
| ✅ 数 `class="ap-layer"` / `class="ap-bar"` **元素** | 0 = 真没弹，1 = 真弹了 |

**恒定注入的模板 / CSS / 占位符会让关键字搜索全绿。** 判据一律取**元素级**（数标签、抠元素内文本），
不要拿「文档里出现过这个词」当「功能实现了」。

> 这条是**审核方在他的 L3 冷启动里送回来的**：他的反例 agent 主动提醒了这个陷阱，
> 否则他那轮「反例没弹层」的结论会被 CSS 里的关键字污染。
> 同类教训还有一条（G36 实现时踩到）：**说明性文字不构成实现**——代码注释/docstring 里提到某功能，
> 不等于代码输出了它；扫描器必须**剥注释 + 只看输出语句**。

---

## 六、改契约 = 改三处

改渲染管线/交付格式的字段时，三处必须同步：

1. 函数 **docstring** 的写法清单（机器提取的就是它）
2. **SKILL.md** 的 JSON 契约表
3. **门禁 fixture**（测试里实际构造的输入）

> **修「代码接受什么」≠ 修「文档承诺什么」。** 反例：代码已能读某字段、文档仍只写 3 个字段 →
> 照文档字面写的人**静默丢字段**；而 fixture 因为「比合同多给一个字段」一直绿着，把缺口盖住。
> **fixture 比合同更全 = 门禁永远绿着掩盖缺口。**

---

## 七、秘密类配置不随包发布

「内部标记清单」「脱敏替换表」这类**列着「要藏什么」的文件，本身就是泄漏面**。
写在包内 → zip / 拷文件夹 / 传给同事会一起带走（`.gitignore` 只挡得住 git 这一条路）。

- 清单放**包目录之外**，审计工具与同步脚本**读同一份**（两处读不同文件 = 两份清单分叉）。
- 三种入口任选：`--config <包外路径>` / `SKILL_AUDIT_CONFIG` / `SKILL_AUDIT_MARKERS`。
- 包内只留形态配置（`profile` / `source_dir` / `skip_dirs`），这些公开无害。

---

## 八、每轮收口固定动作（照着做，不靠记）

```bash
# ① 报告目录有没有新报告（可能没被投喂）
ls <报告目录>

# ② 反模式系统门禁（7 道）
python scripts/verify_anti_patterns.py

# ③ 跨轮回归核验
python scripts/cross_round_check.py

# ④ 静态体检（两版）—— 标记清单走包外配置
python <skill-release-audit>/scripts/skill_audit.py <本包> --profile=local \
  --config <包外配置>
python <skill-release-audit>/scripts/skill_audit.py <本包>/github-export --profile=opensource \
  --config <包外配置>

# ⑤ 差分
python <skill-release-audit>/scripts/skill_diff.py <本包>/github-export

# ⑥ 同步开源包（用脚本，别用 cp）
#    ↓ 本地工作区专有（开源包不含下面这些脚本；文档里以 LOCAL-ONLY 段标记）
python <本地工作区>/scripts/sync_export.py --replace

# ⑦ 两版账本各补条目 → 提交 → 推送

# ⑧ **最后一步**：整份重跑生成证据文件（不要手工拼、不要复用旧块）
python <本地工作区>/scripts/make_evidence.py --round <N>

# ⑨ 回头再跑一次跨轮核验——R12 会确认「证据文件不早于最后一次提交」
python scripts/cross_round_check.py
```

> ⚠️ ⑥⑦⑧ 是**发布流程**，命令名指向的是发布侧工具（`sync_export.py` / `make_evidence.py`），
> 它们**随本 skill 的本地工作区**存在，不在公开包里——公开包读者看方法论即可，不必找这两个脚本。

> ⚠️ **⑦⑧ 的顺序反了就出事**（第十二轮 P1 真实事故）：证据文件生成之后又提交，
> 文件头却写着「本文件为推送后重跑」——**门禁块贴的是修复前的输出，结论行是修复后的**。
> 只读门禁块的人以为被卡住，只读结论行的人以为全绿。详见第十节。

> ⚠️ 审计工具的**项数会随版本变化**（曾 31→33→35→37 项，阻断分母 22→23）。
> **任何脚本里都不要写死这个数字**——写死会在工具升级后永远误报。

---

## 九、往文件里写「含转义符的代码」：用编辑器，别用 heredoc（2026-09-25 立）

实测踩到 **4 次**（同一天内：第九轮两次、第十一轮一次、第十二轮一次），症状完全一样——
用 `python - << 'PYEOF' …` 这类 heredoc 写含 `\n` 的代码时，**`\n` 会被写成 `/n`**，
写出来的文件直接语法错误，而且**报错位置与真实原因隔了一层**（看到的是 `if X:/n  Y`，容易以为是别的毛病）。

- ❌ `python - << 'PYEOF'` + `s.replace(a, "if x://n    y = 1")`
- ✅ **用编辑工具直接改文件**（所见即所得，不经过 shell 转义层）
- 判据：**只要要写的内容里含 `\n` / `\t` / 嵌套引号，就用编辑器**；
  heredoc 只用来跑「不含转义序列」的逻辑（比如读文件、跑检查、打印结果）。

> 这条不是"小心点就行"——同一个人同一天踩四次，说明它是**工具用法问题，不是注意力问题**。

---

---

## 十、证据链完整性：整份重跑 + 时间线自证（2026-09-25 第十二轮 P1 立）

**事故**：一份证据文件在 `00:04:53` 生成，之后 `00:05:27` 又提交了账本改期；
而文件头写着「本文件为推送后重跑」——**里面的门禁块贴的是修复前的输出，结论行是修复后的**。

```
【门禁 2】… G18 账本与文件同步 ← 之前是过的，现在坏了
结论: 🔴 出现回归，禁止发布          ← 第 53 行
…
  差分新回归 0                → ✅    ← 第 93 行
```

**为什么单列**：这不是「数字错了」，是**证据链断了**——一个只读门禁块的人以为被卡住，
一个只读结论行的人以为全绿。而「贴原始输出」这条零号铁律的全部意义，
就是**让原始输出与结论不可能打架**。

**两条硬要求**：

1. **整份重跑，不复用旧块**。收口最后一步跑 `python <本地工作区>/scripts/make_evidence.py --round <N>`，
   它一次生成全部内容（含时间线自证抬头）。**不要手工拼、不要把修复前的输出粘进来**。
   <sub>（该脚本是**本地工作区专有的发布流程工具**，不在公开包内；公开包读者只需沿用它背后的两条要求。）</sub>
2. **文件自证时间线**：抬头必须写明「本文件生成时刻」与「最后一次提交时刻 / 工作区是否干净 /
   有无未推送提交」。**谁早谁晚一眼可见**，不用去翻 mtime。

**且这条有门禁**：`cross_round_check.py` 的 **R12** 检查「最新证据文件 mtime ≥ 最后一次提交时刻」，
早于即报红并提示重跑。第一次写这道检查时它**在本地永远 SKIP**（本地工作区不是 git 仓库，
只有开源包是）——**空转的门禁比没有门禁更坏**，已改为取「有 `.git` 的那一份」历史。

> ⚠️ 本节编号曾因「插入新节时只编号不排序」而乱序（八 → 十 → 九 → 十一），
> 与交付页跳号、卡库 B037 编号断裂是**同一个病**——本轮一并修正。
> **元教训**：`⑦提交 → ⑧生成证据` 这个顺序**不能靠记**。靠记的结果就是两次踩同一个坑
> （第九轮、第十一轮各一次）。凡是「靠记得住」的流程，都要么写成脚本、要么做成门禁。

---

## 十一、门禁不得空转：带自测夹具（2026-09-25 第十二轮立）
**绿有两种**：真查过、什么都没查。二者在输出上长得一样。

实例（写门禁 7 时踩到）：共用词覆盖检查刚上线时，全库关键词交集**恰好是 0 对**——
它报绿，但绿的含义只是「没东西可查」。若哪天交集非空且未登记，它才会报红，
可**在此之前谁也不知道它到底会不会报**。

**做法**：凡是「可能因为没数据而恒绿」的检查，都要带一个**自造夹具**，
喂一份必然违规的假输入，确认它真的报出来：

```python
fake = {"AP-90": {"kw_list": ["共用词X", "甲独有"]},
        "AP-91": {"kw_list": ["共用词X", "乙独有"]}}
assert len(_uncovered_pairs(fake)) == 1     # 检查器必须报出来，否则门禁空转
```

同族纪律：**「没查 ≠ 通过」**（跨轮核验的三态设计，SKIP 单列不并入通过）、
**「报数要带口径」**（第四节）。三条一起守的就是同一件事：**别让绿变成一个没有信息量的字**。

---

## 十二、缺口要闭环：台账 + 状态 + 催办（2026-09-25 第十二轮立）

规格里写着「**▮缺口 = 最值钱的产出**」（SKILL.md）。但它过去一直是
**散落在 case 文件里的一段话**——没有编号、没有状态、没有催办。

**代价是实测出来的**：`case-208`（09-06）提的缺口，
**19 天后的 `case-209`（09-25）又提了一遍**，中间无人跟。

**做法**：本地工作区的**缺口台账**（`缺口台账.md`）收全部缺口，每条带
`编号 / 提出日期 / 来源 / 摘要 / 建议归属 / 状态 / 凭证`；工具是同级的 `gap_ledger.py`。
<sub>（台账与 `gap_ledger.py` 都是**本地工作区专有**——它们引用内部案例内容，不在公开包内。
公开包读者请把这节当**方法论**读：缺口必须有编号、状态、催办；而下面那些判据是通用的。）</sub>

- **提缺口前三步（硬要求）**：① 查现有卡（含子模式）→ ② 查台账 → ③ 再写。
  > 反例：曾把「B083 缺程度档」写成「**库里没有『度』**」，建议「扩 A034 为四档」——
  > 而 A034 标题就是「模态：必然 vs 可能（**含"度"**）」。真缺口是「B083 没有回指 A034/A024」。
  > **照错误边界去立卡 = 把已有的东西再立一遍。**
- **状态不许留白**：`待立卡 / 已立卡 / 已增强 / 暂不新建 / 驳回`。
  后两者**必须写理由**——否则它们就是清空台账的捷径。
- **催办看聚合，不看单条**：`--report` 按**卡号**聚合——某张卡被多条缺口点名 = 该做一次**集中增强**。
  > ⚠️ 口径：**按卡聚合 ≠ 同一条缺口重复提**。两条不同缺口指向同一张卡是正常的。
  > 只有卡号相同**且**指同一处判断才算回头账——**那需要人判，机器不假装能判。**
- **别用文本相似度自动找重复**（试过，已否）：本语料所有缺口都是同一模板写的
  （「…材料 B明示… 现有 B0xx 未明确」），相似度聚的是**模板**不是**主题**——
  0.36 档会把「标题题核心元素法」和「研究结论反推焦点」判成同类。
  **一个看起来聪明但不靠谱的启发式，比没有这个功能更坏**：它会让台账变成噪音源。
- **门禁 R13**：每个声明缺口的 case 都必须在台账里有行。

---

## 十三、字段必须「核出来的」，不是「爬出来的」（2026-09-25 第十三轮立）

**案件**（审核方第十三轮复验，N1/N2）：

台账的「状态」列由**正则从 case 原文推断**（`infer_status`），而规则表把
「已补/已立/已落/**已挂**」排在「已增强」**前面**——于是

> 「已挂 B051 **增强记录**」被「已挂」先命中 → 判成「**已立卡**」。

**23 条「已立卡」里 9 条就这么错的**，状态列**系统性偏乐观**。更硬的是：
台账文档声明的判据写着「…**或卡片确实已含该内容**」，而**后半句没有任何实现**（纯爬文本）。

**三条教训**：

1. **爬出来的字段只能保证「非空」，保证不了「为真」。**
   规则里「A 或 B」这种写法，若 B 没实现，**要立刻改文档**——否则它就是一纸空头承诺
   （本项目的 **G33**：文档承诺 vs 实现，同型）。
2. **顺序即优先级**：`(已补|…|已挂)` 写在 `(…增强…)` 前面，就等于宣布「已挂优先」——
   写的时候没人想过这件事。**多条规则并存时，要问一句「谁赢」并写下来。**
3. **门禁只验「最弱的那个性质」= 假绿。** R13 原来只验两件事：
   「声明缺口的 case 都有行」+「状态列非空」——**这两条恰好是「爬取式生成」必然满足的性质**，
   所以它永远绿，且绿得毫无信息量。
   → 现在补到 **6 项**（含违规判决 + **自测夹具**），见第十一节。

### 关键设计：把「不可判的语义问题」转成「可判的字符串问题」

「内容到底有没有落地」这件事，**用关键词搜索判不了**。实测过两种自动判据（26 条已落地行）：

| 判据 | 结果 |
|---|---|
| 松：引号词组切分后任一命中 | 报 13 条，**5 条假阳性**（卡片是**改写**的措辞，不是原文） |
| 严：引号词组 2 字滑窗 | 报 **0** 条 —— 完全空转 |

**所以不判语义，只要求「可定位」**：状态写「已立卡/已增强」的行，备注里必须有一条
**凭证** `核：2026-09-25 B010 §极性判断`，机器只验三件确定性的事：

- 归属卡文件存在（`B010` → `references/cards/B-skills/010-*.md`）
- 凭证里的卡号 == 归属列（防「归属 B058、凭证写 B062」）
- `§` 后的锚点**逐字出现在该卡里**

给不出凭证 → 它确实没落地。**你签名，就得能引一句卡里的原文。**
（首次落地：20 条拿到凭证、2 条内容确实没有 → 回退 `待立卡`、4 条定位不到 → `待核` 人工队列。）

同族纪律：**「待核」是合法状态**，门禁不为它报红——它是**给人排队的队列**
（`gap_ledger.py --verify` 逐条催办）。门禁管「不许假称已落地」，人管「把它真落地」。

### 附带抓到的一条：台账摘要被硬截断

`summarize(t, 72)` 让 **161 行里 106 行（66%）**变成半句话，而且截断会把
「→ 已落 B0xx」的**卡号切掉**（`GAP-130` 就是被切掉的）。
**台账的第一功能是「以后还能拿它干活」**——截断直接把这个功能砍了。
现默认**不截断**，并加了 `--refresh-summary` 把历史行按「同 case 内唯一前缀匹配」补回
（配不上就列出待人工，**不猜**）。

---

## 十四、文档里的路径必须存在：本地专有内容不进公开包（2026-09-25 第十三轮立）

**案件**（审核方 N3）：公开包的 `SKILL.md` / `VERIFICATION.md` 指示读者去跑
`gap_ledger.py`、`sync_export.py`、`make_evidence.py` 三个脚本，
以及 `缺口台账.md`——**这 4 个资产一个都不在公开包里**（它们是本地发布流程工具，
脚本自己的注释就写着「永不同步」）。第三方照做只会 `file not found`。

**根因**：两版文档长期**手工对齐**，而「两版关键文件一致」那个检查**只覆盖 5 个脚本、不含文档**
→ 谁都没看见。**靠「同步时记得删」是挡不住的，得靠机制。**

**做法（两条并存）**：

1. **段标记 + 自动剥离**：文档里把本地专有内容包进
   `<!-- LOCAL-ONLY:BEGIN -->` … `<!-- LOCAL-ONLY:END -->`；
   `sync_export.py` 同步文档时**剥掉整段并插入一句「本节为本地工作区专有流程，已略去」**。
   —— 机制化的「删」，不是人肉记得住的「删」。
2. **就地标注**：不方便分段的（如收口命令清单），用一行 `<sub>` 明写
   「**本地工作区专有，公开包不含**」。

**兜底门禁**：审计工具 **G39**（文档引用的包内路径必须存在）——只扫操作说明类文档，
在开源包上精准命中 3 处，其余 4 个对象零误报。

---

## 十五、说明性文字必须可查：别写「机制生效了」（2026-09-25 第十二轮立）

交付件/账本里的**说明性文字**最容易顺手写「**某机制生效了**」——但它**不可查**。

真实反例（审核方评审 L4 交付件时抓出）：

> ❌ 交付件里写：「未弹 AP-02：与 AP-03 共用『语感』类词，**按互斥对只留更实的一条**」
>
> **实测**：那条自述里 AP-02 的关键词**一个都没命中**，根本轮不到互斥对出手；
> 而且自第十一轮切分后两张卡**已不共用任何关键词**，那条登记**当时处于空转状态**。

**规则**：机制叙事**必须能对应到一次实测**。写不出实测，就改写**事实**：

> ✅ 「未弹 AP-02：**它没有任何命中**（该自述只命中了 AP-03 的『凭感觉』）」

同族教训：**「修好一个实例」≠「机制已完备」**（第十二轮）——
立了通用机制（互斥表）之后**必须补一次覆盖检查**，否则第二例照旧中招。
本条与它是一对：前者管**写的时候**别编，后者管**改的时候**别只改一个。

---

## 十六、验收工具不归我们改：要 patch，不要直接落盘（2026-09-26 第十四轮立）

**事故**（本轮审核方报的，我方认账）：整改期间我们**直接改了验收方的工具**——
`skill-release-audit/scripts/skill_audit.py`（G33 判据脆弱点、G38 加夹具改口径）与它自己的 `SKILL.md`。

**改得对不对？两处都对**，其中 G33 那处**是审核方自己的 bug**（`[^}]*` 读嵌套结构必然截断），
而且正好被我们新增的选项渲染踩到——**等于替他们修 bug**。方向也正是他们上一轮报的问题。

**但流程上仍然越界，理由三条**（审核方原话）：

1. **验收工具是独立性的根。** 一个能改裁判评分表的人，会让**所有绿灯失色**——哪怕他改得对。
   这不是防我们，是防这套流程本身失去可信度。
2. **复核代价是实在的。** 他们必须多做一道工序：回头确认「改动是否只限于声明的那两处、
   有没有顺手放宽别的判据」（本轮他们做了：检查项集合无增删、`v1.10` 标记只出现在两处、
   40 项全对象复跑结果一致 → **无夹带**）。**但这道工序本来不该存在。**
3. **同一条边界，两边都踩过**：第十轮审核方为测 G32 误改了我方开源包的 `_audit.json`，
   当时他们认账并完整还原。**这次是反方向——同一条线，两边都得守。**

**规则（我方执行，硬要求）**：

> **发现验收工具有问题 → 出 patch，不落盘。**
> 指出 bug / 给出 diff / 写清复现条件都欢迎；**由工具 owner 改、由 owner 升版本、由 owner 记来源**。
> 我们在**自己的会话里**照同一份逻辑实现可以（自己包内的门禁本就是自己的），
> 但**不碰验收方的文件**——哪怕一个字符。

**已发生的处置**：工具 owner 已核验并**正式合入**那两处（版本记为 **v1.10.2**，
在 SKILL.md 里注明「由被审方提出、验收方核验后合入」）。
→ 本轮的 23/23 仍是**旧版口径**跑的；后续复验以 owner 的版本为准。

**同族纪律**：**「改对了」不构成「可以改」。** 与第二节「跨轮回归核验」、第十一节「门禁不得空转」
是一组——都在问同一个问题：**这个结论是谁在什么权限下得出的？**

---

## 十七、这些纪律的来路（一句话版）

| 纪律 | 从哪次事故来 |
|---|---|
| 基线独立实现 | 对比表差点变成废纸（第九轮） |
| 跨轮回归核验 | 修好 8 条误报、同时打坏一类漏报（第七→八轮） |
| 用例追加式、分来源 | 「漏报 0/15」在外部样本上只有 5/9（第六轮） |
| 报数带口径 | 只举报一种口径 = 假绿（第七轮） |
| 判据标实测 | 两次「说得很肯定但没测过」被推翻（第八、九轮） |
| 改契约改三处 | 同一处契约缺口两轮复发（第七、八轮） |
| 配置不随包 | 「列着要藏什么的清单」差点被 zip 带走（第九→十轮） |
| 验实物要验到元素级 | 恒定注入的 CSS 让 `grep` 全绿（第十一轮） |
| 整份重跑 + 时间线自证 | 证据文件里门禁块说红、结论行说绿（第十二轮） |
| 门禁带自测夹具 | 覆盖检查在「交集恰好为 0」时报绿＝没查（第十二轮） |
| 通用机制要补覆盖检查 | 立了互斥表只登记一对，第二对又漏（第十二轮） |
| 写代码用编辑器别用 heredoc | 同一天把 `\n` 写成 `/n` 四次（第十二轮） |
| 缺口要闭环 | 同一条缺口 19 天后被重提，中间无人跟（第十二轮） |
| 说明性文字必须可查 | 交付件写「按互斥对只留更实的一条」，实际一条都没命中（第十二轮） |
| 卡库卫生 | 卡片整块重复、标题编号断裂，肉眼难发现（第十二轮） |
| 字段要「核」不要「爬」 | 状态列正则顺序错 → 23 条「已立卡」里 9 条口径错（第十三轮） |
| 门禁别只验最弱性质 | R13 只验「状态非空」，而那正是爬取式生成的必然结果（第十三轮） |
| 文档路径必须存在 | 公开包教读者去跑 3 个**包里没有**的脚本（第十三轮） |
| 验收工具只能由验收方改 | 我们直接改了审核方的 `skill_audit.py`（**改对了**也得走 patch）（第十四轮） |
| 队列必须有时限 | 3 条 `待核` 可无限期挂着而门禁每轮报绿＝回头账的第二形态（第十四轮） |
