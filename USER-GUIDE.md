# 阅读分析 Skill · 使用说明书

> **这是什么**：一个装进 AI 里的"英语阅读分析老师"——把任何英语阅读题（高考/四六级/考研）喂给它，它会按五阶段分析：宏观结构 → 逐题溯源 → 干扰项构造 → 对撞复盘 → 卡片反馈。喂得越多，它越懂你的错题规律。
> **适用**：任何人，手机或电脑都行；不需要会编程。

---

## 一、怎么装进 AI（三种方式，任选）

### 方式 A：网页对话直接粘（最简单，5 秒）
1. 打开任意 AI 对话框（豆包/DeepSeek/Kimi/ChatGPT 等）
2. 打开仓库根目录的 `SKILL.md`（约 1.6 万字符），全选 → 复制
3. 粘进对话框，回车
4. 之后直接发题就行。**每次开新对话要重新粘一次**（AI 不记旧对话）

> 只想试试、不打算长期用 → 用这条。
> 想让它真的变强（能挂卡库、能入库、能复盘）→ 用方式 B，方式 A 拿不到 `references/` 里的卡。

### 方式 B：装成 Agent / Skill（一劳永逸，推荐）
适用于任何支持 skills / 能读文件的工具（WorkBuddy / Claude Code / Cursor / ZCode / Codex / Gemini CLI …）：

1. **把整个仓库拿到本地**：`git clone`，或网页上点 `Code → Download ZIP` 后解压
2. **装的是整个文件夹，不是单个文件** —— 它要读 `references/` 里的卡库，只放 `SKILL.md` 会残缺
3. **放进你所用工具的 skills 目录**。不知道目录在哪，按这个顺序试：
   - 先看工具的说明文档里 skills / rules 目录在哪（WorkBuddy 是 `~/.workbuddy/skills/<名字>/`）
   - **找不到就退回最通用的办法**：把这个文件夹放在你的**项目目录**里，然后对 AI 说
     「读 `SKILL.md`，按它干活」—— 一样能跑，只是不会自动触发
4. **装完开一个新会话**（工具通常在启动时扫描 skills 目录，当前会话不一定能发现新装的）
5. 之后直接说"分析这篇阅读"即可，AI 会自动调用

### 方式 C：作为资料库参考（API / 框架）
- `SKILL.md` 作为 system prompt
- `references/cards/`（127 张方法卡）挂成知识库（RAG）—— 卡库是这套东西的主体，146 个文件 / 850 KB
- `references/QUESTION-TYPE-MAP.md` 是「题型 → 该查哪张卡」的对照表

适合自己搭 agent 的人。

---

## 二、怎么提问（照着发就行）

### ① 喂一道错题（最常用）——三步投喂法

| 步 | 给 AI 什么 | 怎么说 |
|---|---|---|
| ① 文章 | 原文全文 | 「文章在这里：粘贴原文」 |
| ② 错题 | 哪题 + 你选的 + 正确答案 | 「第 N 题，我选了 X，正确答案是 Y」 |
| ③ 思路 | 你当时为什么选错（**最重要**） | 「我当时以为……因为看到……所以选了 X」 |

第③步说出来，AI 才能告诉你错在哪一步，而不是只给答案。

### ② 直接甩整篇（没做错也想精读）
> 「帮我分析这篇阅读：粘贴原文 + 题目」

AI 会做**完整精读**：宏观结构、每段功能、逐题解析、干扰项为什么错、长难句剥离。

### ③ 只问一道题（最短路径）
> 「第 2 题为什么不选 A？粘贴题目选项」

### ④ 追问（随便问，问到底）
> 「这段怎么概括出来的」「这个干扰项怎么构造的」「为什么这句是主旨句」

### ⑤ 复盘（每做完 10 题来一次）
> 「阅读复盘」

AI 会统计你的错误类型分布，找出重复犯的错，总结成你的个人规律。

### ⑥ 投喂新题型/新理论
> 「投喂：粘贴材料」——材料会被提炼进卡片体系，喂得越多 AI 越强。

---

## 三、你会拿到的交付物

每篇分析完，你会收到**双交付**：
- **分析结果.html**（网页版，可分享，术语可点开）——给人看的
- **分析结果.json**（结构化数据）——给网站导入存档用的（以后能在网站上跨篇统计你的错题规律）
- 底部有 💬 追问提示和 ⭐ 评分条

---

## 四、评分与反馈（请配合）

- 每次分析完给个分：**回复「X 星」**（1-5 星）
- **低分（3 星以下）务必说哪里不满意**——这是 AI 改进的唯一通道，不满意会被记进"进化日志"，下次就不一样了

---

## 五、常见问题

| 问题 | 答案 |
|---|---|
| 模型上下文不够，粘不下 `SKILL.md`？ | 分两次粘：先粘规则（`SKILL.md`），再发题；或用方式 B 装成 skill |
| 每次开新对话要重粘吗？ | 方式 A 要；方式 B 不用（装进工具了） |
| AI 给的答案和老师不一样？ | 很正常——把两边都贴给 AI，让它对比分析 |
| 隐私：我的错题会被存吗？ | 看你用的 AI 工具；本地安装的方式 B 数据不出你电脑 |

---

*版本：见 `SKILL.md` 的 `version:` 字段（不在此处写死，免得两处对不上）· 本说明书描述的就是仓库里的内容，无额外附件*

---

## 批量跑题（一次 5-10 篇）—— 主战场模式

**怎么发**：一次给 AI 5-10 篇题目（PDF/截图/文本都行），说一句「批量跑」即可，不用逐篇下指令。

**拿到什么**：对话里每篇只有五行速览（篇章模式/答案对照/用到卡/缺口/一句话病灶），详析全部落 case 文件在 `references/cases/`。

**跑完看什么**：①五行里的【▮缺口】栏——那是发现卡库漏洞的地方 ②对撞表里的遗漏要点 ③喊主会话「整合」——新规则落卡、成绩入账本。

---

## 说人话就行（模式映射表）

| 你说 | AI 走的模式 |
|---|---|
| 「分析这道题」/ 贴一道题 | A 单题分析（强制挂卡+缺口栏） |
| 「拆这篇文章」/ 贴一篇文章 | F 全文拆解（强制挂卡+缺口栏） |
| 「跑这篇，有书解析」或喂了参考答案 | G 对抗式（闭卷→对撞→升级卡） |
| 「批量跑」+ 5-10 篇 | 批量速览档（五行/篇，详析落 case） |
| 「阅读复盘」 | 复盘：错因统计+卡片评审 |
| 「投喂」+ 新题/理论 | 入库流程（编号登记→闭卷排期） |
